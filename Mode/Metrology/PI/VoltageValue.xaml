<Window x:Class="Mode.Metrology.PI.VoltageValue"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mode.Metrology.PI"
        xmlns:Data ="clr-namespace:UI.Controls.Settings.DeviceConfig.Base.BaseSettingsConfig;assembly=UI"
  
        mc:Ignorable="d"
        Title="DeviceBusCommutationWindow" 
  
        Height="200" 
        Width="400"
        MaxHeight="200"
        MaxWidth="600"
        WindowStyle="None" 
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Topmost="True" 
        WindowStartupLocation="CenterScreen"
        Background="Transparent">

    <Border Background="{StaticResource IsCheckedColorSolidColorBrush}" CornerRadius="30">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="60"/>
                <RowDefinition />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Введите эталонное значение в В." Foreground="{StaticResource ForegroundSolidColorBrush}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <Border Grid.Row="1" Background="{StaticResource PrimarySolidColorBrush}" CornerRadius="5" Margin="20,10">
                <TextBox 
                     Background="Transparent" 
                     BorderThickness="0" 
                     VerticalAlignment="Center" 
                     FontSize="20" Foreground="{StaticResource ForegroundSolidColorBrush}"
                     PreviewTextInput="NumberDevice_PreviewTextInput"
                     KeyDown="VoltageInput_KeyDown"
                    Name="VoltageInput"
                     />
            </Border>
            <Button Grid.Row="2"
        Content="Сохранить" 
        Style="{StaticResource ConfigButtonStyle}" 
        HorizontalAlignment="Center" 
        Width="250"
        Margin="20,0"
        PreviewMouseDown="SaveButton_Click"/>


        </Grid>

    </Border>
</Window>   
