<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:docking="clr-namespace:UI.Windows.WpfDocking.Windows.Docking"
                    xmlns:primitives="clr-namespace:UI.Windows.WpfDocking.Windows.Docking.Primitives">


    <Style TargetType="primitives:DocumentTab">
        <Setter Property="ToolTip" Value="{Binding Description}" />
        <Setter Property="primitives:DockManager.TargetItem" Value="{Binding}" />
        <Setter Property="primitives:DockManager.TargetPosition" Value="Tab" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="DoubleClickCommand" Value="{x:Static docking:DockCommands.ToggleFloating}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="primitives:DocumentTab">
                    <ControlTemplate.Resources>
                        <SolidColorBrush x:Key="BorderBrush" Color="#4D4D4D" />
                        <SolidColorBrush x:Key="UnselectedBrush" Color="#2E2E2E" />
                        <SolidColorBrush x:Key="SelectedBrush" Color="#4b7765" />
                        <SolidColorBrush x:Key="MouseOverBrush" Color="#3A3A3A" />
                    </ControlTemplate.Resources>

                    <Border x:Name="border"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="0"
                            CornerRadius="10,10,0,0"
                            Background="{StaticResource UnselectedBrush}"
                            Padding="5,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock x:Name="textBlock"
                                       VerticalAlignment="Center"
                                       Foreground="White"
                                       FontSize="15"
                                       FontWeight="Normal"
                                       Text="{Binding TabText}"
                                       TextTrimming="CharacterEllipsis"
                                       TextWrapping="NoWrap" />

                            <Button Grid.Column="1"
                                    Width="16"
                                    Height="16"
                                    Margin="5,0,0,0"
                                    Padding="0"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Cursor="Hand"
                                    Command="{x:Static docking:DockCommands.PerformClose}">
                                <Path Data="M 0 0 L 8 8 M 8 0 L 0 8"
                                      Stroke="White"
                                      StrokeThickness="2"
                                      Stretch="Uniform" />
                            </Button>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="UIElement.IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource MouseOverBrush}" />
                        </Trigger>
                        <DataTrigger Binding="{Binding Path=IsSelected}" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource SelectedBrush}" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
