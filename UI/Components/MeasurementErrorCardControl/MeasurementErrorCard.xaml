<UserControl x:Class="UI.Components.MeasurementErrorCardControl.MeasurementErrorCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Components"
             xmlns:Icon="clr-namespace:UI.Icon"
             mc:Ignorable="d" 
             d:DesignHeight="220" d:DesignWidth="300">
             
    <Border Background="{StaticResource ActiveBorderSolidColorBrush}" CornerRadius="20">
        <Border Background="{StaticResource PrimarySolidColorBrush}" CornerRadius="20" Margin="5,0,0,0" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="TitleTextBlock" Text="Команда" FontSize="20" FontWeight="Bold" Foreground="White" Grid.Row="0" VerticalAlignment="Center"/>
                    
                    <StackPanel  Grid.Column="1">
                        <Icon:SuccessIconControl x:Name="SuccessIcon" Width="24" Height="24" Visibility="Collapsed" Margin="5,0,0,0"/>
                        <Icon:ErrorIconControl x:Name="ErrorIcon" Width="24" Height="24" Visibility="Collapsed" Margin="5,0,0,0"/>
                    </StackPanel>
                </Grid>

              

                <Grid Grid.Row="1" Margin="0,10,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <local:TextBoxPlaceholder x:Name="PercentageInput" VerticalAlignment="Center" Margin="0,0,5,0" Height="40" Background="{StaticResource SecondarySolidColorBrush}"/>
                    <TextBlock Text="%" VerticalAlignment="Center" Foreground="White" Grid.Column="1" FontSize="16"/>
                </Grid>

                <!-- Числовая погрешность -->
                <Grid Grid.Row="2" Margin="0,10,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <local:TextBoxPlaceholder x:Name="NumericInput" VerticalAlignment="Center" Margin="0,0,5,0" Height="40" Background="{StaticResource SecondarySolidColorBrush}"/>
                    <TextBlock x:Name="UnitTextBlock" Text="..." VerticalAlignment="Center" Foreground="White" Grid.Column="1" FontSize="16"/>
                </Grid>


                <!-- Кнопка -->
                <Button x:Name="SaveButton" Content="Сохранить"  Grid.Row="3" Style="{StaticResource CompareButtonStyle}" Margin="0,10" PreviewMouseDown="SaveButton_PreviewMouseDown" />

            </Grid>
        </Border>
    </Border>
</UserControl>
