<UserControl x:Class="UI.Components.SearchControls.WholeWordToggleButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Components.SearchControls"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="25">
    <Border x:Name="Border"  BorderThickness="0,2,0,2"  BorderBrush="{StaticResource ForegroundSolidColorBrush}">
        <Button x:Name="ToggleButton"
             Content="Abl"
             FontSize="16"
             FontWeight="Bold"
             Background="Transparent"
             Foreground="{StaticResource ForegroundSolidColorBrush}"
             BorderThickness="0"
             Padding="0"
             HorizontalAlignment="Center"
             VerticalAlignment="Center"
             Focusable="False"
             PreviewMouseDown="ToggleButton_PreviewMouseDown">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid Background="Transparent">
                        <ContentPresenter HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       TextElement.FontWeight="Bold"
                                       TextElement.FontSize="16"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Отключаем фон при наведении -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Border>
</UserControl>
