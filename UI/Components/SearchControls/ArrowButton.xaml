<UserControl x:Class="UI.Components.SearchControls.ArrowButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UI.Components.SearchControls"
             mc:Ignorable="d"
             Width="24" Height="24">
    
    <UserControl.RenderTransform>
        <RotateTransform x:Name="RootRotate" Angle="0" CenterX="12" CenterY="12"/>
    </UserControl.RenderTransform>

    <Grid>
        <Button x:Name="ArrowButtonControl"
                Background="Transparent"
                BorderThickness="0"
                Padding="0"
                Width="24" Height="24"
                Cursor="Hand"
                Focusable="False"
                Click="Button_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Rectangle Fill="Transparent"/>
                        <Viewbox>
                            <Canvas Width="24" Height="24">
                                <Path x:Name="ArrowPath"
                                      Stroke="{Binding ArrowColor, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      StrokeThickness="2.5"
                                      Data="M 6 10 L 12 16 L 18 10"
                                      StrokeLineJoin="Round"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round"
                                      Cursor="Hand"/>
                            </Canvas>
                        </Viewbox>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ArrowPath" Property="Stroke" 
                                    Value="{Binding HoverArrowColor, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</UserControl>
