<UserControl x:Class="UI.Components.CloseButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Components"
             mc:Ignorable="d" 
               d:DesignHeight="20" d:DesignWidth="20" MinWidth="20" MaxWidth="20" MinHeight="20" MaxHeight="20">

    <Border Background="Transparent" Margin="2" CornerRadius="10" MouseEnter="BackgroundBorder_MouseEnter" MouseLeave="BackgroundBorder_MouseLeave" x:Name="BackgroundBorder">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="Transparent"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="Red"/>
                        <Setter Property="Cursor" Value="Hand"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid>
            <Rectangle Width="15" Height="2" Fill="{StaticResource ForegroundSolidColorBrush}" RadiusX="1.5" RadiusY="1.5"
               RenderTransformOrigin="0.5,0.5" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="45"/>
                </Rectangle.RenderTransform>
            </Rectangle>
            <Rectangle Width="15" Height="2" Fill="{StaticResource ForegroundSolidColorBrush}" RadiusX="1.5" RadiusY="1.5"
               RenderTransformOrigin="0.5,0.5" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="-45"/>
                </Rectangle.RenderTransform>
            </Rectangle>
        </Grid>
    </Border>

</UserControl>
