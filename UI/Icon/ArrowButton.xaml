<UserControl x:Class="UI.Icon.ArrowButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UI.Icon"
             mc:Ignorable="d"
             d:DesignHeight="32" d:DesignWidth="32"
             Width="32" Height="32">

    <Button x:Name="PART_Button"
          Cursor="Hand"
          Padding="0"
          Background="Transparent"
          BorderThickness="0"
          Click="Button_Click"
          FocusVisualStyle="{x:Null}"
          Command="{Binding RelativeSource={RelativeSource AncestorType=local:ArrowButton}, Path=Command}"
          CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=local:ArrowButton}, Path=CommandParameter}">
        <Button.Template>
            <ControlTemplate TargetType="Button">
                <Grid>
                    <!-- Плитка -->
                    <Border x:Name="Tile"
                  CornerRadius="8"
                  Background="{Binding ButtonBackground, RelativeSource={RelativeSource AncestorType=local:ArrowButton}}"
                  BorderBrush="{Binding ButtonBorder,     RelativeSource={RelativeSource AncestorType=local:ArrowButton}}"
                  BorderThickness="1"/>

                    <!-- Стрелка (уменьшенная и тонкая) -->
                    <Path x:Name="Glyph"
                Width="10" Height="10"
                Stretch="Uniform"
                Stroke="{Binding ArrowBrush, RelativeSource={RelativeSource AncestorType=local:ArrowButton}}"
                StrokeThickness="2"
                StrokeLineJoin="Round"
                StrokeStartLineCap="Round"
                StrokeEndLineCap="Round"
                Fill="Transparent"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                RenderTransformOrigin="0.5,0.5">
                        <Path.RenderTransform>
                            <RotateTransform Angle="0"/>
                        </Path.RenderTransform>
                        <Path.Data>
                            <PathGeometry Figures="M 2,2 L 6,6 L 10,2"/>
                        </Path.Data>
                    </Path>
                </Grid>

                <ControlTemplate.Triggers>
                    <!-- Hover: меняем цвет штриха -->
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="Glyph" Property="Stroke" Value="{Binding ArrowHoverBrush, RelativeSource={RelativeSource AncestorType=local:ArrowButton}}"/>
                    </Trigger>

                    <!-- Поворот через анимацию -->
                    <DataTrigger Binding="{Binding IsArrowUp, RelativeSource={RelativeSource AncestorType=local:ArrowButton}}"
                       Value="True">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="Glyph"
                                   Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                   To="180" Duration="0:0:0.15">
                                        <DoubleAnimation.EasingFunction>
                                            <QuadraticEase/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                        <DataTrigger.ExitActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="Glyph"
                                   Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                   To="0" Duration="0:0:0.15">
                                        <DoubleAnimation.EasingFunction>
                                            <QuadraticEase/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.ExitActions>
                    </DataTrigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Button.Template>
    </Button>
</UserControl>
